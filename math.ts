export const sumar = (a: number, b: number): number => {
  return a + b
}
export const multiplicar = (a: number, b: number): number => {
  return a + b
}
export const restart = (a: number, b: number): number => {
  return a + b
}
export const dividir = (a: number, b: number): number => {
  return a + b
}